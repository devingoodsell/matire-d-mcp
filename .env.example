# Maître d' MCP — Remote Deployment Environment Variables
#
# Copy to .env and fill in values:
#   cp .env.example .env

# ── Required ─────────────────────────────────────────────────────────
# Pre-shared bearer token for MCP endpoint authentication.
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(48))"
MCP_AUTH_TOKEN=

# Master encryption key for the credential database.
# Generated by: python -m src.setup
RESTAURANT_MCP_KEY=

# ── Cloudflare Tunnel ────────────────────────────────────────────────
# Token from Cloudflare Zero Trust dashboard (Networks → Tunnels → Create → get token).
# Required only when using docker-compose with cloudflared.
TUNNEL_TOKEN=

# ── Optional ─────────────────────────────────────────────────────────
# Override defaults (usually not needed — set in docker-compose.yml)
# MCP_TRANSPORT=streamable-http
# MCP_HOST=0.0.0.0
# MCP_PORT=8000
# LOG_LEVEL=INFO
